specVersion: "v1beta"
serviceId: "firbaseRTC"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firbaseRTC-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
